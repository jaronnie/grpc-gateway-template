syntax = "proto3";
package app1;
option go_package = "./mypb/app1";

import "google/api/annotations.proto";
import "hello.proto";

service app1 {
  // Sends a greeting
  rpc SayHello (Hello) returns (Hello) {
    option (google.api.http) = {
      post: "/api/v1/hello"
      body: "*"
    };
  }
}